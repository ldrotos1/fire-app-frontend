import { UnitTypeNamePipe } from './unit-type-name.pipe';

describe('UnitTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new UnitTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
